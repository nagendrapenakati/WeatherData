package com.example.weatherapp.data.mapper

import com.example.weatherapp.data.model.WeatherModel

class WeatherMapper {
    fun mapToWeather(weatherModel: WeatherModel) : WeatherModel{
        return weatherModel
    }
}